/* LEER EL SCRIPT POR ORDEN NUMÉRICO */

// 5. FUNCIÓN QUE SE EJECUTA CUANDO BACKEND HA RESPUESTO
const onSuccesResponse = () => {
  // OBTENEMOS LOS DATOS DE BACKEND. EN UN CASO REAL data NOS VENDRÍA COMO PARÁMETRO
  const data = 'BACKEND RESULTS';
  console.log('SUCCESS', data);
};

// 2. ESTÁ ES LA FUNCIÓN QUE HARÁ LA PETICIÓN SIMULADA A BACKEND
const retrieveInfoFromBackend = () => {
  /* 3. EL SETTIMEOUT SIMULA EL TIEMPO QUE TARDARÍA BACKEND EN CONTESTAR, CUANDO HAYAN PASADO 3 SEC 
  SE LLAMARÁ A LA FUNCIÓN onSuccesResponse */
  setTimeout(onSuccesResponse, 3000);
};

//1. AQUÍ EMPIEZA EL SCRIPT, SIMULAREMOS UNA LLAMADA A BACKEND QUE DEVUELVE UNOS RESULTADOS
retrieveInfoFromBackend();
